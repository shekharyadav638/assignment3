import accordion from './accordion.twig';

import accordionData from './accordion.yml';

import './accordion';

export default { title: 'Molecules/accordion' };

export const acc = () => accordion(accordionData);
